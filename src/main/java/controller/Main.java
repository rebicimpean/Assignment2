package controller;

import java.awt.EventQueue;

import view.*;

public class Main {
	public static void main(String[] args) {

		MainGui mainFrame = new MainGui();

		// ShopEvolution shop = new ShopEvolution("Empty Shop");
		// Controller controller = new Controller(30, 2, 8, 4, 12, 2);
		// Thread t = new Thread(controller);
		// t.start();
		// RandomClientGenerator rand = new
		// RandomClientGenerator(30,2,8,4,12,controller);
		// Thread randThread = new Thread(rand);
		// randThread.start();
	}
}
